import { Project, RateioMensal, Vencimento, User } from './types';

export const USERS: User[] = ["Joey", "Joedge", "Spencer"];

// Dados limpos para iniciar o sistema do zero
export const PROJECTS: Project[] = [];

export const RATEIOS: RateioMensal[] = [];

export const VENCIMENTOS: Vencimento[] = [];